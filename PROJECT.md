# stock-monitor-v2

PayPay証券で保有している銘柄を Web 上で管理し、毎朝その評価額・損益・売買判定をメールで受け取るためのアプリ。

既存の「株価モニター（v1）」は GitHub Actions + Python で動く単一銘柄のスクリプトだった。v2 ではブラウザから銘柄を追加・編集・削除できるようにし、複数ユーザー（自分と友人）が各自の銘柄を管理できるようにする。

---

## 目的

- 銘柄を変えるたびにコードを直接編集する手間をなくす（フォームから入力）
- 売却・買い増しをコード変更なしで反映できるようにする
- 友人にも使ってもらえるよう、ユーザーごとにデータを分ける
- Django の実践経験を積み、ポートフォリオとして公開できる成果物にする

---

## 技術スタック

- **バックエンド**: Django（Python）
- **データベース**: 開発時は SQLite（設定不要・ファイル1個で完結）、デプロイ時に PostgreSQL へ切り替え
- **株価取得**: yfinance（Yahoo Finance）
- **定期実行 + メール送信**: 既存の GitHub Actions の仕組みを流用（毎朝 JST 9:00 = cron `0 0 * * *`）
- **デプロイ先候補**: Fly.io（無料枠あり）

### なぜこの構成か
- SQLite から始めるのは、開発初期に DB サーバーの準備で詰まらないため。Django はDB を後から差し替えやすい設計になっている。
- 株価チェックとメールは v1 で完成済みなので、作り直さず連携させる方が早い。

---

## データ設計（保有銘柄）

PayPay証券は「金額指定（◯円分）」で買えるため、保有株数は小数になる。
日本株は yfinance で「証券コード + .T」で取得する（例: 任天堂 = 7974.T）。

1銘柄が持つ情報（案）:
- `name`: 会社名（例: 任天堂）
- `ticker`: yfinance 用ティッカー（例: 7974.T / SOXL）
- `market`: 市場区分（"jp" or "us"）— 計算方法とメール表示の出し分けに使う
- `shares`: 保有株数（小数。例: 1.0035545904）
- `cost`: 投資元本（円）。日本株はこれで損益を計算
- `buy_price`: 買付額（米国株 SOXL のように元本ではなく取得単価で持っているケース用。設計時に jp/us を統一するか検討）
- `owner`: 所有ユーザー（誰の銘柄か。マルチユーザーのため必須）

### 計算方法
- **日本株**: 評価額 = 現在株価 × shares、損益 = 評価額 − cost、リターン% = 損益 / cost × 100
- **米国株（SOXL）**: v1 のロジック（current × shares と buy_price の比較）。jp と計算式を揃えられるか実装時に検討。

### 現在の保有データ（v1 から引き継ぎ・初期データ）
| 会社名 | ティッカー | market | shares | cost / buy_price |
|--------|-----------|--------|--------|------------------|
| SOXL | SOXL | us | 160.36 | buy_price=41726 |
| 任天堂 | 7974.T | jp | 1.0035545904 | cost=10000 |
| 日立製作所 | 6501.T | jp | 0.9075014067 | cost=4500 |

※ eMAXIS/PayPay証券 全世界バランス（投信・コード03312249）は yfinance で取得困難なため対象外。

---

## 開発フェーズ（小さく作って育てる）

学習目的のため、一気に全部作らず段階的に進める。各フェーズで「動くもの」を完成させる。

### フェーズ1: 自分1人用の CRUD
- ブラウザのフォームから銘柄を追加・表示・編集・削除できる
- ログインなし、メールなし、ローカル（自分のPC）で動くだけ
- ここで Django の基礎（モデル / ビュー / フォーム / テンプレート / URL ルーティング）を一通り習得する
- **このフェーズの完成がプロジェクトの土台**

### フェーズ2: ログイン認証 + ユーザーごとのデータ分離
- 各ユーザーがログインし、自分の銘柄だけを見られる / 編集できる
- 友人にも使ってもらえるようにする核心部分
- Django 標準の認証機能（django.contrib.auth）を使う

### フェーズ3: 株価取得とメール送信
- yfinance で現在株価を取得し、評価額・損益・判定を計算
- 既存の GitHub Actions（毎朝 cron + メール送信）と連携
- 連携方式: Django 側に「銘柄リストを JSON で返す API」を作り、GitHub Actions がそれを読む案を軸に検討
- メールはユーザーごとに各自のアドレスへ送る

### フェーズ4: デプロイ（公開）
- Fly.io などの無料枠へデプロイ
- DB を PostgreSQL に切り替え
- 環境変数（メール認証情報など）の安全な管理（v1 では GitHub Secrets を使用）

---

## セキュリティ上の注意

- 銘柄データ（買値・株数）は金銭に関わる個人情報。誰でも閲覧・編集できる状態にしない。
- メール送信用のパスワード等は Gmail アプリパスワードを使い、コードに直書きしない（環境変数 / Secrets で管理）。
- マルチユーザーでは「他人のデータが見えない」ことを必ずテストする。

---

## 既存資産（v1）からの引き継ぎメモ

- リポジトリ: stock-price-monitor（kakun02250225-ship-it）
- GitHub Actions のワークフロー（schedule.yml）: cron `0 0 * * *`(JST 9:00) + workflow_dispatch（手動実行可）+ メール送信ステップ（dawidd6/action-send-mail@v3）
- メール用 Secrets: MAIL_USERNAME / MAIL_PASSWORD（Gmailアプリパスワード）/ MAIL_TO
- yfinance はバージョン固定せず最新を使う（v1 で 0.2.35 固定だとデータ取得に失敗した経緯あり）

---

## 開発時の心がけ（自分用メモ）

- コードの意味を理解しながら進める。動けばいい、ではなく「なぜこう書くか」を確認する。
- 分からない部分は飛ばさず、その都度調べる / 質問する。
- 各フェーズが動いたら git でコミットして区切る。
